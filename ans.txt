q1  
function prints first then `10`

q2  
`fn()` loses `obj` but `obj.getName()` gives `"Harsh"`

q3  
it prints `5` five times use an iife to save each `i`

q4  
it prints `"John"` because frozen objects cannot change

q5  
`==` converts values `===` checks value and type

q1 reverse every word  
split words reverse each one then join them back

q2 remove duplicate characters  
loop chars and add only if it is not already in result

q3 custom map()  
loop array run callback push results into a new array

q4 flatten array  
use recursion and keep adding normal values to one array

q5 deep clone  
make new arrays or objects and copy nested values again

q1  
output is start end promise timeout

q2  
return a promise that resolves after the given time

q3  
track results and resolve when every promise is done

q4  
start all fetches together and wait until all are done

create a todo app  
keep tasks in an array show them in dom save in localstorage

find every bug  
add `return` in both `filter` and `map` callbacks

implement debounce  
clear old timer and run only after user stops calling it

implement throttle  
run once then block calls for some time

write your own eventemitter  
store callbacks by event name and run them on emit

implement memoization  
save old results and reuse them for same input

implement groupby()  
use age as key and push matching people into that group